kind: Pod
apiVersion: v1
metadata:
  name: env-test
spec:
  containers:
    - name: nginx
      image: nginx
      env:
        - name: ENVIRONMENT
          value: "development"
        - name: PROJECT
          value: "Kubernetes Course"